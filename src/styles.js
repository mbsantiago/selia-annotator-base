const BASE_STYLE = {
  strokeStyle: "yellow",
  lineWidth: 1,
};

const LIST_STYLE = {
  lineWidth: 1,
};

const HOVER_STYLE = {
  strokeStyle: "cyan",
  lineWidth: 4,
};

const CREATE_STYLE = {
  lineDash: [10, 15],
  strokeStyle: "red",
  lineWidth: 4,
};

const EDIT_STYLE = {
  strokeStyle: "yellow",
  lineWidth: 6,
};

const DELETE_STYLE = {
  strokeStyle: "red",
  lineWidth: 4,
};

const STYLES = {
  LIST_STYLE,
  HOVER_STYLE,
  CREATE_STYLE,
  EDIT_STYLE,
  DELETE_STYLE,
};

export { STYLES };
